---
"@bldg-7/proxmox-mcp": minor
---

Add QEMU agent tools: file read/write, get hostname, get users, set user password, and guest shutdown via QEMU guest agent (6 new tools). Includes new validateFilePath and validateUsername validators.
